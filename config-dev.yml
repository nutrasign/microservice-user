environment:
  STAGE: ${opt:stage, self:provider.stage}
  REGION: ${opt:region, self:provider.region}
  APP: 'nutrasign'
  PASSWORD_LENGTH: '4'
  SESSION_EXPIRATION: '10h'
  SIGN_ALGORITHM: 'HS512'
  TOKEN_SECRET: 'nutrasign'
